---
import ApproachPage from '../../components/ApproachPage.astro';
import { approaches, detailApproachSlugs, type Approach } from '../../data/approaches';
export function getStaticPaths() {
  return approaches
    .filter((approach) =>
      typeof approach.slug === 'string' &&
      detailApproachSlugs.has(approach.slug)
    )
    .map((approach) => ({
      params: { slug: approach.slug },
      props: { approach },
    }));
}

interface Props {
  approach: Approach;
}

const { approach } = Astro.props;
---

<ApproachPage {approach} />
